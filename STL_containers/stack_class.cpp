#include <iostream>
#include <stack>
#include <string>
using namespace std;
// Write a program that implements a stack class with the following operations:
// push(int x): add element 'x' to the stack
// pop(): remove and return the top element
// peek(): return the top element without removing it
// isEmpty(): check if the stack is empty
int main() {

    return 0;
}
