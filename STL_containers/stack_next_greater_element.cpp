#include <iostream>
#include <stack>
#include <string>
using namespace std;
// Write a program that finds the next greater element of each element in the array, using
// stack to keep track of elements, if no greater element, return -1
int main() {
    return 0;
}
